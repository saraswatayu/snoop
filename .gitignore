__pycache__/
*.py[cod]
*$py.class
.venv/
.pytest_cache/
.coverage
*.egg-info/
.DS_Store
.snoop/
*.swp
/tmp_*

# Local tooling / scratch dirs — never committed.
.benchmarks/
.claude/
.gstack/

# Internal docs (designs, plans, specs) — keep out of the public repo. Local-only.
docs/
TODOS.md

# Calibration fixture overrides may contain real targets' email addresses.
# Local-only; never committed.
tests/fixtures/calibration_targets.local.json
tests/fixtures/calibration_report.md

# Eval fixtures distilled from real cases may contain real identities while
# being fictionalized, and eval runs transcribe whatever bundle they graded.
# Local-only; never committed.
tests/evals/fixtures/*.local.json
tests/evals/runs/
