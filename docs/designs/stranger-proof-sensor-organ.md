---
status: ACTIVE
---
# CEO Plan: snoop 100x — the Stranger-Proof Sensor Organ
Generated by /plan-ceo-review on 2026-06-11
Branch: refactor/prime-time | Mode: SCOPE EXPANSION
Repo: saraswatayu/snoop

Design doc (source of truth for baseline scope):
`~/.gstack/projects/saraswatayu-snoop/ayush-refactor-prime-time-design-20260611-085916.md`
(APPROVED, 3 adversarial review rounds, 14 issues fixed, final 10/10)

## Vision

### 10x Check
Approach B makes snoop trustworthy anywhere; the 10x past that is snoop
**provably right and visibly improving**: identity binding strong enough that
the card explains *why* it's the right person, and a measurement loop where
every real use sharpens probe ordering and grows calibration. The numbers
become the marketing; the marketing regenerates itself. With E1 accepted, this
plan ships that whole arc in one branch.

### Platonic Ideal
You think of a person and type one line. Thirty seconds of sensor time later
you have what a great chief of staff would have prepped: who they are, the one
address that works, the best way in, and a reason to believe every single
line — misses labeled as honestly as hits. It never asks what it can figure
out, never claims what it can't cite, and works identically on your laptop and
a stranger's. Using it makes it better — each week's hit rate is a little
higher than last week's, and it can prove that too.

### Eureka (landscape)
Contact-tool incumbents' stored database is their liability (20–25%/yr decay,
junk inference); an LLM with live typed sensors needs no database for
single-target lookup — freshness and grounding by construction. snoop is
live-sensing vs stored-copy, a different category, not a cheaper Hunter.
Scoped honestly (premise 2 revised after the Codex challenge): this holds for
targets *with a public trail*; for thin-trail targets snoop's value is the
honest blank instead of a stale guess.

## Scope Decisions

| # | Proposal | Effort (human/CC) | Decision | Reasoning |
|---|----------|--------|----------|-----------|
| E1 | Sensor-yield ledger / proof lab (Approach C pulled forward, **descoped per T1.2**) | ~2wk / 2–3d (auto-apply machinery removed) | **ACCEPTED (revised)** | User overrode the defer recommendation; after the outside-voice challenge and the T1 ledger design memo, settled as: **archetype ledger, offline-first** — RunRecord fields + `plan_shape` booleans + `mx_class` enum + date-only timestamps (all under the 3C allowlist); ordering analysis is offline, ordering changes are deliberate evidence-cited commits; **auto-applied ordering graduates to phase 2 only with evidence**. README hit rates come from calibration (truth-labeled); the ledger contributes yield/latency trends only. The design-pass collapse stands: D14 + the allowlist test (= written schema) land before ledger code. |
| E2 | rel="me" / Bluesky identity-binding sensor | ~2d / 0.5d | **ACCEPTED** | Strengthens the anchor-binding core every other feature leans on; bidirectional self-attestation, zero ToS surface. |
| E3 | PGP keyserver sensor (keys.openpgp.org) | ~1d / 2–3h | **ACCEPTED** | Owner-verified positives by construction; discovers and corroborates (esp. M365-inconclusive addresses). |
| E4 | SKILL.md worked-example archetypes ×3 | ~0.5d / 1–2h | **ACCEPTED** | First-attempt quality is the product (premise 1); offset token growth by trimming fallback prose. |
| E5 | --redact flag for shareable cards | ~0.5d / 1–2h | **SKIPPED** | User: redaction is anti-product — the point is surfacing real verified info. Demos use consenting subjects. |
| E6 | Per-sensor timing + real wall-clock deadline | ~1–2d / 0.5d | **ACCEPTED** | Makes the ≤60s success criterion enforceable code, not hope; fixes the known run_pipeline timeout defect (docs/plans/2026-06-10-prime-time-review.md §4 item 4, snoop.py:458–469); timing feeds E1 and calibration. |

Held back (lower priority, user may request): GitLab sensor, personal-site
channel-link extraction, meeting-prep render mode, marketplace packaging.

## Accepted Scope (this plan = design doc baseline + accepted expansions)

Baseline (Approach B, per the design doc):
- Zero-friction loop: SKILL.md self-plan contract; Google-probe auto-arm logic
  gated behind `--allow-google-account` until the consent flow (M2
  diagnose-first first-run) ships, then ungated.
- Typed degradation contract: sensor_status `ran|skipped|degraded` + reason,
  mapped onto ResolverResult.status (`ok|empty→ran`,
  `timeout|unavailable|error→degraded`, never-invoked→`skipped`, SMTP
  `unprobed`→`skipped`); `--ground` renders "not checked because…" lines.
- First-run flow: diagnose-first on fresh machines; anonymous-GitHub 60 req/hr
  named degradation case; dnspython missing → `skipped: dependency dnspython
  missing`.
- Honest M365 enrichment: getuserrealm.srf NameSpaceType as provider context,
  never a mailbox claim.
- Per-(candidate, source) provenance preserved in structured `data`, plus the
  Milestone 3 provenance audit.
- Windows: distinct `unsupported` diagnose status (vs `missing`).
- Distribution: one-command install with dependency handling (dnspython
  installed or honestly degraded) per the design doc's Distribution Plan —
  in scope for this branch (README + degradation behavior).
- Calibration: fixture to N≥25 real public-trail targets under the provisional
  ground-truth protocol; one supervised live run; measured per-sensor hit
  rates in README.

Expansions accepted this session:
- E1 sensor-yield ledger (local, yield metadata only — never third-party
  data), archetype-aware probe ordering, calibration growth from real use
  (consent-gated), README hit rates regenerable from the ledger.
- E2 rel="me" bidirectional verification + Bluesky domain-handle sensor.
- E3 keys.openpgp.org verified-identity sensor.
- E4 three worked-example archetypes in SKILL.md (founder-with-HN,
  dev-with-GitHub, exec-on-M365), token-offset by trimming.
- E6 shared wall-clock deadline with true cancellation + per-sensor elapsed-ms
  stamped into the bundle.

## NOT in scope
- --redact / any output masking (user: anti-product).
- Bulk mode of any kind (identity fence).
- M365 mailbox-existence probing (spike memo Option A stands; GetCredentialType
  fabricates positives, OneDrive probe is enumeration-flavored with worthless
  negatives, Teams needs auth).
- Pseudonym de-anonymization, location/family/sensitive-attribute inference
  (2026-06-01 CEO plan fences, re-ratified as premise 4).
- GitLab sensor, channel-link extraction, meeting-prep mode, marketplace
  packaging (held back, not rejected).
- Third-party-data caching of any kind (premise 2; the E1 ledger stores
  sensor yield metadata only).

## Deferred to TODOS.md
- (none — E1 was pulled into scope; held-back items are recorded above rather
  than deferred with commitment)

## Premises (ratified this session; full provenance in the design doc)
1. The product is the Claude-side experience; CLI ergonomics are non-goals.
2. Live sensing beats stored DBs **for public-trail targets**; honest blanks
   elsewhere — and we say so. (Revised per Codex challenge.)
3. Honesty contract: no probe whose failure mode is a confident false
   positive.
4. Safety fences stay: one target per invocation, no bulk, self-published
   under real name only.
5. First-run on a stranger's machine is product scope.
6. Credibility requires measurement (N≥25 live calibration gate).

## Implementation decisions resolved up front (0E)
- **Ledger home & posture (user decision D14):** on by default at
  `~/.snoop/ledger.jsonl` — outside the repo, no git-accident path — with a
  strict schema (sensor name, status, elapsed ms, outcome class; never target
  names, addresses, or handles) and one honest README sentence acknowledging
  the default-on local file.
- **Bundle schema versioning:** add `"schema": 2` to the bundle so `--ground`
  and the SKILL.md examples can assert the shape they expect.
- **Deadline default (E6):** 60s shared wall-clock budget, 2s per-sensor
  floor, `--deadline` flag to tune.
- **rel=me scope v1 (E2):** personal-site ↔ Mastodon / Bluesky / GitHub
  bidirectional links only.
- **Probe-ordering heuristic** (replacing the deleted scorer's `belongs~`
  ghost): non-pattern source > pattern-only, then source count — until the E1
  ledger supersedes it with measured ordering.
- **Design-doc Open Questions 2–4** (consent-UX wording, getuserrealm opt-out
  flag, stranger-machine detection): deliberately left open; resolved during
  implementation at their milestones (M1, M3, M2 respectively).
- **In-branch merge order & honest total:** baseline B (~1 CC-week) + E1
  (4–5 CC-days) + E2/E3/E4/E6 (~2 CC-days) ≈ 2.5–3 CC-weeks on one branch.
  Sequencing inside the branch: M1 loop contract → calibration sprint →
  M2 degradation/consent → M3 enrichment/provenance → E2/E3/E6 sensors &
  deadline → E1 ledger last (it consumes E6's timing data and the calibration
  baseline). Each stage lands green before the next starts (WIP commits per
  continuous checkpoint mode).

## Review findings incorporated (Sections 1–10, all user-approved)
- **1A** Ledger writes are best-effort/never-fatal: stderr warning only; card
  and exit code unaffected; `--diagnose` reports ledger health.
- **1B** One `RunRecord` type in lib/schema.py serializes into bundle timing,
  ledger lines, and calibration rows.
- **2A** `--ground` rejects schema-v1 bundles with a named error + "re-run
  --observations" hint.
- **2B** `deadline-exceeded` is a distinct degradation reason from
  sensor-timeout; resolver crash isolation (snoop.py:412) pinned by a
  regression test.
- **3A (+T3)** Shared fetch helper with SSRF guards: private-IP block with
  **pinned-resolved-IP connections** (DNS-rebinding-proof), HTTPS-only, ≤3
  redirects re-checked per hop, **streamed decompressed-size cap** +
  content-type gate (zip-bomb-proof).
- **3B** `_oneline` control-char collapse mandated for all new bundle/card
  text (PGP UIDs, rel=me titles) with marker-forgery tests. (Prior learning
  applied: profile-card-marker-injection, 9/10.)
- **3C** Ledger key-allowlist + value-pattern CI test enforces the
  no-target-data schema (now including `plan_shape`, `mx_class`, date-only
  `ts`).
- **4A** Zero-observation "honest blank" card is a designed, tested empty
  state (shows checked / not-checked / why).
- **4B** Ledger appends are single-write O_APPEND JSONL lines <4KB (atomic by
  construction); reader skips + counts malformed lines.
- **5A** `_employer_match`/`_ORG_SUFFIX_TOKENS` extracted to lib/normalize.py
  **before** E2 lands (verified duplication: gh_search.py:48–64,
  person_resolve.py:85–111).
- **6A** Deadline chaos test (hung resolver: budget held, deadline-exceeded
  reported, other results survive) is E6's acceptance gate.
- **8A** One-line stderr run summary rendered from RunRecords.

## Outside-voice resolutions (Codex plan challenge, all user-decided)
- **T1/T1.2** E1 descoped to archetype-ledger offline-first (see revised E1
  row). The ledger design memo (in the review transcript) settles that
  archetype learning needs only plan-shape booleans + MX class + date-only
  timestamps — no target data.
- **T2** E6 mechanism reworded honestly: deadline on waits + cancel queued
  futures + **abandon running stragglers** (daemon threads) + per-call socket
  timeout audit as the true backstop. Python cannot kill blocking threads;
  the plan no longer claims it can. Same ≤60s contract; 6A unchanged.
- **T5** Calibration fixture privacy: real-person ground truths go ONLY in
  gitignored `calibration_targets.local.json`; the public fixture holds
  EXAMPLE/synthetic rows + explicitly-consented entries; CI asserts this;
  deletion-on-request retention line added to the protocol. README discloses
  that exact numbers reproduce only against local fixtures.
- **T-minor A** README methodology framing: "measured on N targets of these
  classes, on DATE" — measurements, never accuracy claims.
- **T-minor B** M365 NameSpaceType renders visually separate from any address
  (provider context must not read as address validation).
- **T-minor C** Per-sensor evidence-tier table: rel=me, PGP, and Bluesky
  observation classes each get an explicit [+]/[?] tier + reject path for
  partial/stale/colliding identity evidence.
- Rejected/absorbed: "one branch is bad delivery" (merge-order note + WIP
  checkpoints stand), "CLI non-goals contradiction" (machine contract ≠ human
  ergonomics), "SKILL.md platform risk" (the skill file IS the product per
  premise 1).

## Eng-review amendments (2026-06-11, both user-approved)
- **ENG-1 (T6 respec):** the self-plan contract is a **resolution loop**, not a
  capped pass — resolve → sense → if the bundle carries `resolution_gaps`
  (snoop.py:696), resolve more and re-run; done when gaps are quiet. The
  "≤2 WebSearch queries" cap in SKILL.md is deleted. Zero-friction means zero
  *human* friction and maximal *host* resolution (dogfood lesson:
  resolution-is-the-engine, 22→32 observations). SKILL.md's loop diagram
  updates to show the loop; T16's examples include a gap-driven re-run trace.
- **ENG-2 (3B scope):** the forgery defense covers **both** marker
  vocabularies — `[+]`/`[?]` fact-level provenance marks AND the `✓`/`~`/`·`
  identity band (render.py:57) — one collapse rule for every untrusted string
  entering a card line; T9/T10 forgery suites attempt both glyph sets.
- **ENG-3 (T-A, loop bound):** the resolution loop exits on gaps-quiet OR 2
  re-resolve rounds OR a round adding no new observations — whichever first.
  The engine stays maximal; pathological targets terminate in ≤3 passes.
- **ENG-4 (T-B, projections):** RunRecord stays one type with three explicit
  projection serializers (`to_bundle_timing`, `to_ledger_line`,
  `to_calibration_row`), each with a CI leak test asserting its exact field
  set. The 3C allowlist test becomes the ledger projection's test.
- **ENG-5 (T-C group 1, ledger & concurrency contracts):** ledger appends are
  one `os.write()` of pre-encoded bytes with an `<4KB` assert; the file opens
  `O_NOFOLLOW`, mode 0600 in a 0700 `~/.snoop/`; all three artifacts (bundle,
  ledger line, calibration row) carry their own version field and every
  consumer asserts its input's version; the deadline uses a generation token —
  results from abandoned stragglers are never merged after the deadline; the
  per-call timeout inventory is an E6 **precondition** (audit every resolver's
  socket/subprocess calls before the rewrite starts).
- **ENG-6 (T-C group 2, fetch & sensor semantics):** the pinned-IP fetch
  verifies TLS against the ORIGINAL hostname via SNI (`server_hostname=`) —
  certificate verification is never disabled; first-hop http→https upgrade is
  allowed for the personal site itself, off-site profile fetches are
  HTTPS-only; on multi-profile pages only bidirectional pairs bind and
  conflicting bidirectional identities are an ambiguity signal, not a binding;
  Bluesky domain handles require runtime DID resolution tied to the claimed
  profile; the PGP tier is **email-control evidence (deliverability axis)** —
  never identity-binding by itself (three-axes lesson).
- **ENG-7 (T-C group 3, test infrastructure):** lib/_fetch.py ships with a
  shared fake-HTTP test harness all fetch sensors use (SSRF / timeout /
  redirect / cap / parse cases in one place); one end-to-end acceptance test
  runs the documented SKILL.md flow against the real CLI with monkeypatched
  network; calibration is **two runs** — baseline now (the Assignment,
  validates harness + current sensors), final run at branch end producing the
  README numbers.

## The Assignment (carried from the design doc, amended per T5)
Fill the calibration fixture to N≥25 real public-trail targets under the
provisional ground-truth protocol — real-person entries in the gitignored
`tests/fixtures/calibration_targets.local.json` ONLY — and run one supervised
live `python3 -m tests.calibration` — this week. Everything else sequences
behind the measured number.

---
comments:
  c1:
    body: >-
      The ongoing attention I've been attracted to in the Spookmon game is the
      system of AI. If you look at some of the astrodes, maybe we've been
      sourcing telemetry astrogames or thinking about them. The thing that keeps
      coming up is that we're not using AI to explain AI. If you all wanted to
      explore some scary AI projects, then ideally what we're doing is giving
      you all an AI tool list to be able to do a lot of the research on its own
      and to verify that it actually reads a bar, etc.


      My question for you is more so around how are we coming up with AI
      protection, and are we really leaning in on the right set of things?
    by: user
    at: 2026-06-11T14:15:22.303Z
  c2:
    body: >-
      Reading your question as: "are we giving the AI the right tool list to do
      the research on its own and verify it actually did the reading — and is
      our AI-protection thinking right?" Answer in the plan's terms: every
      accepted item is (a) I/O the AI can't do — probes, strengthened by E2
      rel=me, E3 PGP, getuserrealm; (b) contract — typed degradation + timing +
      schema v2, so the AI can reason about what its sensors did; or (c)
      measurement — calibration + ledger. Zero judgment code added; judgment
      stays in the host model. "Research on its own" = the SKILL.md self-plan
      contract (Claude runs WebSearch, builds the plan, decides probes, no
      human authoring). "Verify it read the bar" = --ground, extended this
      branch (schema gate, not-checked-because lines, designed honest blank).
      AI protection is two-directional here: --ground protects the human from
      the AI's uncited claims; finding 3B (_oneline forgery defense on PGP
      UIDs / fetched pages) protects the AI from the data — fetched text is
      treated as prompt-injection surface aimed at the analyst, not just card
      text. Deliberate non-AI spots: probe ordering and ground truth stay
      deterministic/human-gated until ledger evidence earns automation (T1.2).
      If this misreads your question, mark it up and I'll re-engage.
    by: AI
    at: 2026-06-11T14:25:00.000Z
    re: c1
